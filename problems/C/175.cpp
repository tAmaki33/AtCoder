#include <iostream>
#include <string>
#include <vector>
#include <math.h>
#include <algorithm>
using namespace std;

int main(){
  long long  x, k, d;
  cin >> x >> k >> d;
  long long ans = x;
  
  long long count = k - abs(x)/d;
  long long pro;

  if(x < 0) {

  } else {

  }
  // abs(x) / d
  cout <<  abs(x)/d << endl;
  //cout << abs(ans) << endl;
}