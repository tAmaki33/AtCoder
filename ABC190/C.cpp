#include <iostream>
#include <string>
#include <vector>
#include <math.h>
#include <algorithm>
using namespace std;

int main(){
  int n, m;
  cin >> n >> m;
  vector<int> a(m);
  vector<int> b(m);
  for(int i=0;i<m;++i) cin >> a[i] >> b[i];
  int k;
  vector<int> c(k);
  vector<int> d(k);
  for(int i=0;i<k;++i) cin >> c[i] >> d[i];

  int res = 0;
  for(int i=0;i<k;++i) {
    
  }
  cout << res << endl;

}