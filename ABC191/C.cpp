#include <iostream>
#include <string>
#include <vector>
#include <math.h>
#include <algorithm>
using namespace std;

int main(){
  int h, w;
  cin >> h, w;
  vector<vector<string>> s(h, vector<string>(w));
  for(int i=0;i<h;++i) {
    for(int j=0;j<w;++j) cin >> s[i][j];
  }
  
  for(int i=0;i<h;++i) {
     for(int j=0;j<w;++j) {
       if()
     }
  }
}